#include<iostream>
#include<math>
using namespace std;
struct PhanSo {
    int tu, mau;
};
int main() {
    PhanSo P;
    cout << "Nhap tu so: "; cin >> tu;
    cout << "Nhap mau so: "; cin >> mau;
    cout << "Phan so vua nhap la: " << tu so << "/" << mau so << \n;
    return 0;
}
