#include<stdio.h>
#include<conio.h>
struct DuongThang
{
    float a;
    float b;
    float c;
};
typedef struct DuongThang Dt;
void NhapDuongThang(DT &);
void XuatDuongThang(DT);

void NhapDuongThang(DT &dt)
{
    float temp;
    printf("Nhap he so a: ");
    scanf("%f", &temp);
    dt.a = temp;
    printf("Nhap he so b: ");
    scanf("%f", &temp);
    dt.b = temp;
    printf("Nhap he so c: ");
    scanf("%f", &temp);
    dt.c = temp;
}
void XuatDuongThang(DT dt)
{
    printf("%8.3fx + %8.3fy + %8.3f = 0", dt.a, dt.b, dt.c);
}
int main()
{
    DT dt;
    NhapDuongThang(dt);
    XuatDuongThang(dt);
    getch();
    return 0;
}
